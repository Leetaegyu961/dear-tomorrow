# PRD: 내일의 나에게 (Dear Tomorrow)

> 오늘의 한 줄 일기와 내일의 나에게 보내는 편지를 매일 쓰고, 다음 날 그걸 받는 서비스

---

## 1. 개요

### 1.1 서비스 한 줄 정의
사용자가 매일 저녁 "오늘의 한 줄 일기"와 "내일의 나에게 보내는 편지"를 쓰면, 다음 날 그 편지가 도착하는 비동기 셀프 메시징 서비스.

### 1.2 핵심 가치
- **오늘을 닫는 의식**: 한 줄로 하루를 정리하고, 내일의 나에게 말을 건넴
- **내일을 여는 동기**: 어제의 내가 보낸 편지를 받으며 오늘을 시작
- **시간의 누적**: 매일의 감정 색이 모자이크로 쌓여 한 달, 한 해의 흐름이 보임

### 1.3 핵심 컨셉
- 진단하지 않음. 판단하지 않음. 그저 어제와 오늘을 이어줌
- Gemini는 보조 역할(감정 색 추출). 핵심은 사용자 자신의 목소리

---

## 2. 타겟 사용자

- **주 사용자**: 하루를 돌아보는 습관을 만들고 싶은 사람
- **사용 맥락**: 매일 저녁(쓰기) + 매일 아침(읽기)
- **사용자 구분**: Supabase Anonymous Sign-In. 첫 방문 시 자동으로 익명 계정 생성, 세션이 브라우저에 저장됨

---

## 3. 핵심 기능

### 3.1 오늘 열기 (홈 화면)
- 어제 내가 보낸 편지가 도착해 있으면 표시
- 어제의 감정 색을 배경/카드로 시각화
- 어제 편지가 없으면 안내 문구 ("오늘부터 시작해요. 내일 아침 첫 편지가 도착할 거예요.")
- 오늘 아직 안 썼으면 → 오늘 쓰기 버튼
- 오늘 이미 썼으면 → "오늘은 이미 썼어요. 내일 도착할 거예요." 안내

### 3.2 오늘 쓰기 화면
- **한 줄 일기 입력**: 오늘 하루를 한 줄로 (placeholder: "오늘 하루를 한 줄로 적어보세요")
  - 최대 200자
- **내일의 나에게 편지 입력**: 자유롭게 (placeholder: "내일의 너에게 하고 싶은 말을 적어보세요")
  - 최대 1000자
- 저장 버튼
- 저장 시:
  1. 일기 텍스트를 Gemini API에 보내 감정 색(HEX + 이름) 추출
  2. DB에 저장 (날짜, 일기, 편지, 색)
  3. 완료 화면으로 이동

### 3.3 캘린더 / 아카이브
- 월 단위 캘린더 뷰
- 날짜별 감정 색이 모자이크처럼 표시 (작성한 날만 색 있음)
- 날짜 클릭 시 그날 일기 + 편지를 모달/상세 페이지로 표시
- 월 이동 가능

---

## 4. 핵심 규칙

### 4.1 "내일"의 정의
- **하루의 기준**: 자정(00:00) 기준
- **저장 날짜**: 작성 시점의 날짜로 저장
- **편지 도착 시점**: 저장 날짜 + 1일의 00:00 이후
- **예시**: 11/28 23:50에 작성 → 11/28자로 저장 → 11/29 00:00부터 열람 가능
- **예시**: 11/29 02:00에 작성 → 11/29자로 저장 → 11/30 00:00부터 열람 가능

### 4.2 하루 한 번 작성
- 같은 날에는 한 번만 작성 가능
- 이미 작성한 날 다시 들어가면 "오늘은 이미 썼어요" 안내 + 작성한 내용 보기 가능 (수정은 불가)
- (이유: 어제의 나가 보낸 편지를 바꿀 수 없어야 의미가 있음)

### 4.3 첫 사용자
- 어제 편지가 없으면 빈 상태 화면 표시
- 첫 일기 작성 유도

---

## 5. 데이터 모델

### 5.1 사용자 (Supabase Auth)
- 별도 `users` 테이블 만들지 않음
- Supabase가 자동으로 관리하는 `auth.users` 테이블 사용
- 첫 방문 시 `supabase.auth.signInAnonymously()` 호출하여 익명 계정 자동 생성
- 세션은 Supabase 클라이언트가 자동으로 브라우저(localStorage)에 저장 및 갱신

### 5.2 테이블: `entries`
```
id          uuid (PK, default gen_random_uuid())
user_id     uuid (FK → auth.users.id, NOT NULL)
date        date              -- YYYY-MM-DD, 작성 시점의 날짜 (KST 기준)
diary       text              -- 한 줄 일기 (최대 200자)
letter      text              -- 내일의 나에게 (최대 1000자)
color_hex   text              -- 예: "#A8C5D6"
color_name  text              -- 예: "차분한 청록"
created_at  timestamptz       -- 실제 작성 시각
```

**제약 조건**:
- `UNIQUE(user_id, date)` — 하루 한 번 작성 제약
- `user_id`는 `auth.users(id)`를 참조 (ON DELETE CASCADE 권장)

### 5.3 RLS (Row Level Security) 정책

`entries` 테이블에 RLS 활성화 후 다음 정책 적용:

```sql
-- RLS 활성화
ALTER TABLE entries ENABLE ROW LEVEL SECURITY;

-- 본인 데이터만 SELECT 가능
CREATE POLICY "Users can view own entries"
ON entries FOR SELECT
USING (auth.uid() = user_id);

-- 본인 데이터만 INSERT 가능
CREATE POLICY "Users can insert own entries"
ON entries FOR INSERT
WITH CHECK (auth.uid() = user_id);

-- (수정/삭제는 MVP에서 막음 — 정책 자체를 만들지 않음)
```

→ 정책이 없는 작업(UPDATE/DELETE)은 RLS가 자동으로 차단함

---

## 6. 화면 / 라우팅

| 경로 | 화면 | 설명 |
|------|------|------|
| `/` | 홈 | 어제 편지 + 오늘 쓰기 진입 |
| `/write` | 오늘 쓰기 | 일기 + 편지 작성 폼 |
| `/calendar` | 캘린더 | 월별 감정 색 모자이크 |
| `/entry/[date]` | 상세 보기 | 특정 날짜 일기/편지 (캘린더에서 진입) |

---

## 7. 화면 흐름

```
[첫 방문]
  ↓ Supabase 세션 확인
  ↓ 세션 없으면 → supabase.auth.signInAnonymously() 자동 호출
  ↓ auth.users에 익명 계정 생성됨, 세션이 브라우저에 저장
[홈]
  ↓ 어제 entry 조회 (RLS가 자동으로 본인 것만 반환)
  ↓ 없으면: "오늘부터 시작해요" 안내
  ↓ 있으면: 편지 + 색 표시
  ↓
  ↓ "오늘 쓰기" 클릭
  ↓
[오늘 쓰기]
  ↓ 일기 + 편지 입력
  ↓ 저장 클릭
  ↓ Gemini 호출 → 색 추출
  ↓ Supabase INSERT (user_id는 auth.uid()로 자동)
  ↓
[완료 안내] → [홈으로]
```

---

## 8. 기술 스택

- **프레임워크**: Next.js 14+ (App Router)
- **언어**: TypeScript
- **DB / Auth**: Supabase (Postgres, 익명 사용으로 Auth는 미사용)
- **LLM**: Gemini API (`gemini-2.0-flash` 또는 무료 티어 모델)
- **스타일링**: Tailwind CSS
- **배포**: Vercel (선택)

---

## 9. Gemini API 사용 명세

### 9.1 호출 시점
- 사용자가 일기를 저장하는 순간, 일기 텍스트를 보내 감정 색을 추출

### 9.2 프롬프트 (예시)
```
다음 한 줄 일기를 읽고, 글쓴이의 감정에 어울리는 색을 하나 골라줘.
결과는 반드시 JSON 형식으로만 답해.

일기: "{diary_text}"

JSON 형식:
{
  "color_hex": "#RRGGBB",
  "color_name": "색의 한국어 이름 (예: 차분한 청록, 따뜻한 산호색)"
}
```

### 9.3 응답 처리
- JSON 파싱 실패 시 기본 색 사용 (`#CCCCCC`, "회색")
- API 호출 실패 시에도 저장은 진행, 색만 기본값

### 9.4 환경 변수
- `GEMINI_API_KEY`: 서버 사이드에서만 사용 (`/api/save-entry` 라우트 내부)
- 클라이언트에 노출 금지

---

## 10. 디자인 가이드

### 10.1 톤
- 차분하고 따뜻한 분위기
- 일기장 같은 느낌. 화려함 지양
- 여백 넉넉히

### 10.2 색
- 베이스: 크림색 또는 아이보리 (`#FAF8F3` 정도)
- 텍스트: 진한 회색 (`#2C2C2C`)
- 강조: 너무 튀지 않는 따뜻한 색 하나
- 사용자의 감정 색은 카드 배경/테두리로 활용

### 10.3 타이포그래피
- 한글: Pretendard 또는 시스템 폰트
- 본문 크기 적당히 크게 (16~18px)

---

## 11. MVP 범위 (오늘 안에 만들 것)

### 11.1 포함
- [x] 익명 사용자 자동 생성 (Supabase Anonymous Sign-In)
- [x] 오늘 쓰기 (일기 + 편지 입력 → Gemini로 색 추출 → DB 저장)
- [x] 홈 (어제 편지 + 색 표시)
- [x] 캘린더 (월 단위, 감정 색 모자이크)
- [x] 특정 날짜 상세 보기
- [x] RLS로 본인 데이터만 접근 보장

### 11.2 제외 (나중에)
- 로그인 / 회원가입
- 알림 (브라우저 알림, 푸시)
- 편지 수정 / 삭제
- 월말 회고 자동 생성
- 편지 톤 선택 옵션
- "오늘의 질문" 제안
- 공유 기능
- 통계 / 인사이트

---

## 12. 엣지 케이스 / 신경 쓸 것

- **자정 넘기는 작성**: 11/29 02:00에 쓰면 11/29자로 저장 (`new Date()` 기준)
- **타임존**: 한국 시간(KST) 기준으로 통일. 서버/클라이언트 모두 KST로 날짜 계산
- **세션 손실**: 사용자가 브라우저 데이터를 지우면 Supabase 세션이 사라져 새 익명 계정이 생성됨. 기존 기록 접근 불가 (MVP에서는 감수)
- **Supabase 대시보드 설정 필요**: Anonymous Sign-In 기능을 Authentication → Providers에서 활성화해야 함
- **Gemini API 실패**: 색은 기본값으로 저장, 일기/편지는 정상 저장
- **빈 입력 방지**: 일기와 편지 둘 다 최소 1자 이상 입력 시에만 저장 가능

---

## 13. 성공 기준 (실습 목적 기준)

- 오늘 쓰고, 내일 받는 핵심 플로우가 작동
- Supabase에 데이터가 잘 쌓임
- Gemini API가 감정 색을 잘 반환
- 캘린더에 색 모자이크가 시각적으로 예쁘게 보임
- 익명 사용자 여러 명이 각자 자기 데이터만 봄

---

## 14. 빌드 순서 제안 (Claude Code용)

1. **프로젝트 셋업**: Next.js 14 + TypeScript + Tailwind 초기화
2. **Supabase 셋업**: 
   - 프로젝트 생성
   - Authentication → Providers에서 **Anonymous Sign-In 활성화**
   - `entries` 테이블 생성 (SQL은 섹션 5.2 참고)
   - RLS 활성화 및 정책 적용 (SQL은 섹션 5.3 참고)
   - 환경 변수 (`NEXT_PUBLIC_SUPABASE_URL`, `NEXT_PUBLIC_SUPABASE_ANON_KEY`) 연결
3. **익명 인증 로직**: 앱 진입 시 세션 확인 → 없으면 `signInAnonymously()` 자동 호출
4. **오늘 쓰기 화면**: 입력 폼 + 저장 로직 (Gemini 미연결 상태로 먼저)
5. **Gemini 연동**: `/api/extract-color` 라우트 + 저장 플로우 연결 (`GEMINI_API_KEY`는 서버 사이드 환경 변수)
6. **홈 화면**: 어제 편지 조회 + 표시
7. **캘린더 화면**: 월 단위 색 모자이크
8. **상세 보기**: 날짜별 일기/편지
9. **디자인 다듬기**: 여백, 타이포, 색 톤 정리
